/**
 * @file apiConfig.ts
 * @description File này chứa các cấu hình trung tâm cho việc gọi API.
 * Thay đổi BASE_URL ở đây sẽ ảnh hưởng đến tất cả các controller.
 */

// Thay đổi URL này khi bạn deploy backend lên một server khác
export const APIs = 'http://localhost:3000/';
